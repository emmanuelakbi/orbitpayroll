# =============================================================================
# OrbitPayroll - Docker Compose Environment Variables
# =============================================================================
# These variables configure the Docker Compose services.
# Copy to .env or set in your shell before running docker-compose.
# =============================================================================

# PostgreSQL Configuration
POSTGRES_USER=orbitpayroll
POSTGRES_PASSWORD=orbitpayroll_dev
POSTGRES_DB=orbitpayroll
POSTGRES_PORT=5432

# Redis Configuration (used with --profile with-redis)
REDIS_PORT=6379

# Connection URLs for application use
# Use these in your app's .env file:
# DATABASE_URL=postgresql://orbitpayroll:orbitpayroll_dev@localhost:5432/orbitpayroll?schema=public
# REDIS_URL=redis://localhost:6379
