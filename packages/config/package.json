{
  "name": "@orbitpayroll/config",
  "version": "0.0.1",
  "private": true,
  "description": "Environment configuration and validation for OrbitPayroll",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
